# Origin of the Monsters API. Inlined into the static export at build time.
NEXT_PUBLIC_API_URL="http://localhost:3101"

# Production only: site-specific ID from Plausible, without the .js suffix.
# Set as a Coolify build variable. Empty disables analytics completely.
# NEXT_PUBLIC_PLAUSIBLE_SCRIPT_ID="pa-REPLACE_WITH_YOUR_SITE_ID"
