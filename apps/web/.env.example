# Origin of the MonstersDNA API. Inlined into the static export at build time.
NEXT_PUBLIC_API_URL="http://localhost:3101"

# Production only: site-specific ID from Plausible, without the .js suffix.
# Required for the production Docker image, as a Coolify build variable.
# Omit locally to keep analytics disabled.
# NEXT_PUBLIC_PLAUSIBLE_SCRIPT_ID="pa-REPLACE_WITH_YOUR_SITE_ID"
